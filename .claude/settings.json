{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --cached --quiet || (git commit -m \"Auto-commit $(date '+%Y-%m-%d %H:%M')\" && git push origin main)",
            "timeout": 30,
            "statusMessage": "Auto-committing & pushing…"
          }
        ]
      }
    ]
  }
}
